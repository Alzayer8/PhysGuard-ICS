# Examples

`run_example.py` analyzes the bundled synthetic train, validation, and test splits and
creates a new directory under `example-workspace/experiments/`. Every invocation receives
a new experiment ID; an existing experiment is never replaced.

```bash
python examples/run_example.py
```
